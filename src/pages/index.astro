---
import Layout from '../layouts/BaseLayout.astro';

const pageTitle = "Settlr | Home";
---

<Layout title={pageTitle}>
  
  <section class="relative w-full h-screen overflow-hidden flex items-center justify-center">
    
    <div class="absolute inset-0 z-0 overflow-hidden">
      <div class="absolute inset-0 bg-slate-950/60 z-10 pointer-events-none"></div> <div class="absolute inset-0 bg-gradient-to-t from-slate-950 via-transparent to-slate-950 z-10 pointer-events-none"></div>
      
      <video id="heroVideo" autoplay loop muted playsinline class="w-full h-[115%] object-cover object-top opacity-90">
        <source src="/videos/intro.mp4" type="video/mp4" />
        Your browser does not support the video tag.
      </video>
    </div>

    <div class="relative z-20 text-center px-4 max-w-5xl mx-auto space-y-6 animate-fade-in-up pb-32">
      <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-indigo-500/20 border border-indigo-500/30 text-indigo-300 text-sm font-bold uppercase tracking-widest backdrop-blur-md mb-4">
        <span class="w-2 h-2 rounded-full bg-indigo-400 animate-pulse"></span>
        System Live
      </div>
      
      <h1 class="text-6xl md:text-8xl font-black text-white tracking-tight leading-tight drop-shadow-2xl">
        Settlr <span class="text-transparent bg-clip-text bg-gradient-to-r from-indigo-400 to-fuchsia-400">Labs</span>
      </h1>
      
      <p class="text-xl md:text-2xl text-slate-300 font-light max-w-3xl mx-auto leading-relaxed drop-shadow-lg">
        Where Academic Rigor meets Decentralized Finance. <br/>
        <span class="text-white font-medium">We engineer the math behind the markets.</span>
      </p>
    </div>

    <button id="audioToggle" class="absolute bottom-10 right-10 z-30 p-4 rounded-full bg-white/10 hover:bg-white/20 border border-white/10 backdrop-blur-md transition-all group focus:outline-none text-white">
        <svg id="iconMuted" class="w-6 h-6 block" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5.586 15H4a1 1 0 01-1-1v-4a1 1 0 011-1h1.586l4.707-4.707C10.923 3.663 12 4.109 12 5v14c0 .891-1.077 1.337-1.707.707L5.586 15z" clip-rule="evenodd" /><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2" /></svg>
        <svg id="iconUnmuted" class="w-6 h-6 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.536 8.464a5 5 0 010 7.072m2.828-9.9a9 9 0 010 12.728M5.586 15H4a1 1 0 01-1-1v-4a1 1 0 011-1h1.586l4.707-4.707C10.923 3.663 12 4.109 12 5v14c0 .891-1.077 1.337-1.707.707L5.586 15z"></path></svg>
        
        <span class="absolute right-full mr-3 top-1/2 -translate-y-1/2 px-2 py-1 bg-black/50 rounded text-xs text-white opacity-0 group-hover:opacity-100 transition-opacity whitespace-nowrap pointer-events-none">
            Unmute Video
        </span>
    </button>

    <div class="absolute bottom-10 left-1/2 -translate-x-1/2 z-20 animate-bounce text-slate-500">
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path></svg>
    </div>
  </section>

  <script is:raw>
    // AUDIO TOGGLE LOGIC
    const video = document.getElementById('heroVideo');
    const audioBtn = document.getElementById('audioToggle');
    const iconMuted = document.getElementById('iconMuted');
    const iconUnmuted = document.getElementById('iconUnmuted');

    audioBtn.addEventListener('click', () => {
      if (video.muted) {
        video.muted = false;
        iconMuted.classList.add('hidden');
        iconUnmuted.classList.remove('hidden');
      } else {
        video.muted = true;
        iconMuted.classList.remove('hidden');
        iconUnmuted.classList.add('hidden');
      }
    });
  </script>

  <section class="relative py-24 bg-slate-950">
    <div class="absolute top-0 left-0 w-full h-full overflow-hidden pointer-events-none">
        <div class="absolute top-1/2 left-1/4 w-96 h-96 bg-indigo-900/20 rounded-full blur-[100px]"></div>
        <div class="absolute bottom-0 right-1/4 w-96 h-96 bg-fuchsia-900/10 rounded-full blur-[100px]"></div>
    </div>

    <div class="container mx-auto px-4 relative z-10">
      <div class="max-w-6xl mx-auto bg-gradient-to-br from-slate-900 to-slate-950 rounded-[3rem] p-8 md:p-16 border border-white/10 shadow-2xl overflow-hidden group hover:border-indigo-500/30 transition-all duration-500">
        
        <div class="grid md:grid-cols-2 gap-12 items-center">
          <div class="space-y-8">
            <h2 class="text-4xl md:text-5xl font-black text-white leading-tight">
              Constructing the <br/>
              <span class="text-transparent bg-clip-text bg-gradient-to-r from-emerald-400 to-cyan-400">T+0 Market.</span>
            </h2>
            <p class="text-lg text-slate-400 leading-relaxed">
              The digitized future of settlement is here. No clearing houses, no delays. 
              Atomic swaps powered by quantitative risk engines. Experience the infrastructure that bridges TradFi and DeFi.
            </p>
            
            <a href="/modelli/platform_market" class="inline-flex items-center gap-3 px-8 py-4 bg-white text-slate-950 rounded-full font-bold text-lg hover:bg-indigo-50 hover:scale-105 transition-all duration-300 shadow-[0_0_20px_rgba(255,255,255,0.3)]">
              <span>Launch Platform Demo</span>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path></svg>
            </a>
          </div>

          <div class="relative h-64 md:h-full min-h-[300px] bg-slate-900/50 rounded-3xl border border-white/5 flex items-center justify-center overflow-hidden">
             <div class="absolute inset-0 bg-[url('https://grainy-gradients.vercel.app/noise.svg')] opacity-20"></div>
             <div class="relative z-10 flex flex-col items-center gap-4">
                <div class="flex items-center gap-8">
                    <div class="w-16 h-16 rounded-xl bg-slate-800 border border-slate-700 flex items-center justify-center text-xs font-mono text-slate-500">ASSET</div>
                    <div class="text-emerald-500 animate-pulse">
                        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7h12m0 0l-4-4m4 4l-4 4m0 6H4m0 0l4 4m-4-4l4-4"></path></svg>
                    </div>
                    <div class="w-16 h-16 rounded-xl bg-slate-800 border border-slate-700 flex items-center justify-center text-xs font-mono text-slate-500">CASH</div>
                </div>
                <div class="px-4 py-1 bg-emerald-500/20 border border-emerald-500/30 rounded text-emerald-400 text-xs font-mono font-bold">
                    SETTLEMENT: INSTANT
                </div>
             </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="bg-slate-900 py-20 border-t border-white/5">
    <div class="container mx-auto px-4 text-center">
      <p class="text-slate-500 font-mono text-sm uppercase tracking-widest mb-10">Scientific Roots & Origins</p>
      
      <div class="flex flex-col md:flex-row items-center justify-center gap-16 md:gap-24 opacity-80 hover:opacity-100 transition-opacity duration-500">
        <div class="group relative">
            <div class="absolute inset-0 bg-white/10 blur-xl rounded-full opacity-0 group-hover:opacity-100 transition-opacity"></div>
            <img src="/img/SNS.png" alt="Scuola Normale Superiore" class="relative h-28 md:h-36 w-auto object-contain grayscale group-hover:grayscale-0 transition-all duration-500 brightness-200 contrast-125" />
        </div>

        <div class="h-12 w-px bg-slate-800 hidden md:block"></div>

        <div class="group relative">
            <div class="absolute inset-0 bg-indigo-500/10 blur-xl rounded-full opacity-0 group-hover:opacity-100 transition-opacity"></div>
            <img src="/img/settlr.png" alt="Settlr" class="relative h-28 md:h-36 w-auto object-contain grayscale group-hover:grayscale-0 transition-all duration-500" />
        </div>
      </div>
      
      <p class="mt-12 text-slate-400 max-w-2xl mx-auto font-light">
        
        We translate complex systems theory into market infrastructure.
      </p>
    </div>
  </section>

  <section class="py-24 bg-slate-950 relative overflow-hidden">
    <div class="container mx-auto px-6 text-center relative z-10">
      <h2 class="text-4xl md:text-5xl font-bold mb-6 text-white">
        Ready to shape the future?
      </h2>
      <p class="text-lg md:text-xl text-slate-400 mb-10 max-w-2xl mx-auto">
        Competitive solutions, quantitative rigor, and innovation without compromise.
      </p>
      <a href="/contacts/"
         class="inline-block px-10 py-5 rounded-2xl text-white font-bold text-lg
                bg-gradient-to-r from-indigo-600 to-purple-700 hover:from-indigo-500 hover:to-purple-600 
                shadow-[0_0_30px_rgba(79,70,229,0.4)] transform hover:-translate-y-1 transition-all duration-300">
        Get in touch
      </a>
    </div>
  </section>

</Layout>

<style>
  @keyframes fade-in-up {
    0% { opacity: 0; transform: translateY(20px); }
    100% { opacity: 1; transform: translateY(0); }
  }
  .animate-fade-in-up {
    animation: fade-in-up 1s ease-out forwards;
    animation-delay: 0.2s; /* Wait slightly for video to load */
  }
</style>